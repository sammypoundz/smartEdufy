# NOTE: the primary source of truth for the server address is now
# frontend/src/config/server.ts – edit SERVER_URL there.
# This file is only a fallback for any code still reading VITE_API_URL.
VITE_API_URL=http://localhost:5000/api